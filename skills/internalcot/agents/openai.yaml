interface:
  display_name: "InternalCoT"
  short_description: "Expose detailed model-authored reasoning"
  default_prompt: "Use $internalcot to enable detailed visible reasoning for subsequent responses."
policy:
  allow_implicit_invocation: false
